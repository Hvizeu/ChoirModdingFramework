package choir.api.room;

/**
 * Stable room families supported by the current Choir room API.
 *
 * <p>The initial API deliberately exposes only a passive, non-employed,
 * non-producing room. Later families require their own compatibility and save
 * gates.</p>
 */
public enum RoomFamily {
	PASSIVE_DECORATION
}
