package choir.api.combat;

/** Execution modes whose final damage semantics have been verified by Choir. */
public enum CombatExecutionMode {
	/** Live settlement combat after vanilla hit, defence, and mitigation resolution. */
	TACTICAL_SETTLEMENT
}
