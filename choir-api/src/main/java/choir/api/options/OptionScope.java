package choir.api.options;

/**
 * Persistence scope supported by the current Choir options implementation.
 * Save-specific settings are deliberately not advertised before a save contract exists.
 */
public enum OptionScope {
	GLOBAL
}
